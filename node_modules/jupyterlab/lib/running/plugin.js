// Copyright (c) Jupyter Development Team.
// Distributed under the terms of the Modified BSD License.
"use strict";
var console_1 = require("../console");
var instancerestorer_1 = require("../instancerestorer");
var filebrowser_1 = require("../filebrowser");
var services_1 = require("../services");
var terminal_1 = require("../terminal");
var _1 = require("./");
/**
 * The default running sessions extension.
 */
var plugin = {
    activate: activate,
    id: 'jupyter.extensions.running-sessions',
    requires: [services_1.IServiceManager, instancerestorer_1.IInstanceRestorer],
    autoStart: true
};
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Export the plugin as default.
 */
exports.default = plugin;
/**
 * Activate the running plugin.
 */
function activate(app, services, restorer) {
    var running = new _1.RunningSessions({ manager: services });
    running.id = 'jp-running-sessions';
    running.title.label = 'Running';
    // Let the application restorer track the running panel for restoration of
    // application state (e.g. setting the running panel as the current side bar
    // widget).
    restorer.add(running, 'running-sessions');
    running.sessionOpenRequested.connect(function (sender, model) {
        var path = model.notebook.path;
        var name = path.split('/').pop();
        if (_1.CONSOLE_REGEX.test(name)) {
            app.commands.execute(console_1.CommandIDs.open, { id: model.id });
        }
        else {
            app.commands.execute(filebrowser_1.CommandIDs.open, { path: path });
        }
    });
    running.terminalOpenRequested.connect(function (sender, model) {
        app.commands.execute(terminal_1.CommandIDs.open, { name: model.name });
    });
    // Rank has been chosen somewhat arbitrarily to give priority to the running
    // sessions widget in the sidebar.
    app.shell.addToLeftArea(running, { rank: 50 });
}
//# sourceMappingURL=plugin.js.map